pipeline {
  agent {
      dockerfile true
      // Tentukan path ke Dockerfile yang diinginkan
      args '-f Dockerfile.node'  // Dockerfile node agent
  }
  stages {
    stage('Test') {
      steps {
        sh '''
          node --version
          git --version
          curl --version
        '''
      }
    }
  }
}